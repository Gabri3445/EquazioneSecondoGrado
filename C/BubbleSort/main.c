#include <stdlib.h>
#include <stdio.h>

int main()
{
  int n = 5;
  int arr[n] = {20, 6, 4, 7, 93};
}

void printArr(int arr[], int size)
{
  for(int i = 0; i < 0; i++)
  {
    printf("%d", arr[i];
  }
}
void bubbleSort(int arr[], int size)
{
  
}
void swap (int *x, int *y)
{
  int temp;
  temp = *x;
  *x = *y;
  *y = temp;
}
